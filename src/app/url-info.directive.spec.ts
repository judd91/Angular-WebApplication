import { UrlInfoDirective } from './url-info.directive';

describe('UrlInfoDirective', () => {
  it('should create an instance', () => {
    const directive = new UrlInfoDirective();
    expect(directive).toBeTruthy();
  });
});
